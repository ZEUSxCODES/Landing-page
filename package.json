{
  "name": "Landing page",
  "version": "1.0.0",
  "description": "A simple web page",
  "main": "index.html",  // This can be your entry HTML file
  "scripts": {
    "start": "http-server -p 3000"  // You may need to install http-server globally (npm install -g http-server)
  },
  "author": "Anmol0700",
  "license": "MIT",
  "dependencies": {
    "http-server": "^0.12.3"
  }
}
